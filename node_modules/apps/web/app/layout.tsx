import type { Metadata } from 'next';
import Providers from './providers';
import './globals.css';

export const metadata: Metadata = {
  title: 'PingWatch — SaaS Uptime Monitoring & Instant Alerts',
  description: 'Know before your users do. Actively poll product URLs on schedule, detect HTTP errors or downtime, and receive instant alerts.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className="bg-background text-white min-h-screen antialiased">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
