'use client';

import React from 'react';
import Link from 'next/link';
import { Sidebar } from '@/components/Sidebar';
import { useAuth } from '@/hooks/useAuth';
import { useRealtimeStatus } from '@/hooks/useRealtimeStatus';
import { Activity, LogOut, Plus } from 'lucide-react';
import { Button } from '@/components/ui/Button';

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const { user, signOut, loading } = useAuth();
  useRealtimeStatus();

  return (
    <div className="min-h-screen bg-[#0D1117] flex selection:bg-[#00FF87]/30 selection:text-[#00FF87]">
      {/* Sidebar */}
      <Sidebar />

      {/* Main Content Area */}
      <div className="flex-1 flex flex-col min-w-0">
        {/* Top Header */}
        <header className="h-16 border-b border-[#30363D] bg-[#161B22]/50 backdrop-blur-sm px-6 flex items-center justify-between sticky top-0 z-20">
          <div className="flex items-center gap-3">
            <span className="text-xs font-mono text-[#8B949E]">WORKSPACE:</span>
            <span className="text-xs font-mono text-white bg-[#0D1117] px-2.5 py-1 rounded-[6px] border border-[#30363D]">
              Production Cluster (US-Central)
            </span>
          </div>

          <div className="flex items-center gap-4">
            <Link href="/dashboard/sites/new">
              <Button size="sm" variant="primary" className="font-mono text-xs gap-1.5">
                <Plus className="h-3.5 w-3.5" /> New Monitor
              </Button>
            </Link>

            <div className="h-4 w-[1px] bg-[#30363D]" />

            <div className="flex items-center gap-2">
              <span className="text-xs text-[#8B949E] hidden sm:inline font-mono">
                {user?.email || 'Active User'}
              </span>
              <button
                onClick={() => signOut()}
                className="p-1.5 text-[#8B949E] hover:text-[#FF4D4F] hover:bg-[#FF4D4F]/10 rounded-[6px] transition-colors"
                title="Log out"
              >
                <LogOut className="h-4 w-4" />
              </button>
            </div>
          </div>
        </header>

        {/* Page Content */}
        <main className="flex-1 p-6 lg:p-8 max-w-7xl w-full mx-auto">
          {children}
        </main>
      </div>
    </div>
  );
}
