'use client';

import React from 'react';
import Link from 'next/link';
import { ChevronLeft } from 'lucide-react';
import { AddSiteForm } from '@/components/AddSiteForm';

export default function NewSitePage() {
  return (
    <div className="space-y-6 max-w-3xl mx-auto">
      <div>
        <Link
          href="/dashboard"
          className="inline-flex items-center gap-1 text-xs font-mono text-[#8B949E] hover:text-white transition-colors mb-3"
        >
          <ChevronLeft className="h-3.5 w-3.5" /> Back to Dashboard
        </Link>
        <h1 className="text-2xl font-bold tracking-tight text-white">
          Add New Monitored Site
        </h1>
        <p className="text-xs text-[#8B949E] mt-1">
          Configure an HTTPS health check probe with automated uptime tracking and incident alerting.
        </p>
      </div>

      <AddSiteForm />
    </div>
  );
}
