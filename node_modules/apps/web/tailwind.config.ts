import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        background: '#0D1117',
        surface: '#161B22',
        border: '#30363D',
        status: {
          up: '#00FF87',
          down: '#FF4D4F',
          unknown: '#F0A500',
        },
      },
      fontFamily: {
        sans: ['Inter', '-apple-system', 'BlinkMacSystemFont', 'Segoe UI', 'Roboto', 'sans-serif'],
        mono: ['JetBrains Mono', 'Fira Code', 'monospace'],
      },
      borderRadius: {
        button: '6px',
      },
      keyframes: {
        pulseDown: {
          '0%, 100%': { transform: 'scale(1)', opacity: '1', boxShadow: '0 0 0 0 rgba(255, 77, 79, 0.7)' },
          '50%': { transform: 'scale(1.08)', opacity: '0.9', boxShadow: '0 0 0 8px rgba(255, 77, 79, 0)' },
        },
      },
      animation: {
        'pulse-down': 'pulseDown 2s infinite cubic-bezier(0.4, 0, 0.6, 1)',
      },
    },
  },
  plugins: [],
};

export default config;
