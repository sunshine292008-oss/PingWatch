name: pingwatch-schedule
schedule: "* * * * *"          # every 1 minute
timeZone: "UTC"
httpTarget:
  uri: https://run.googleapis.com/v1/namespaces/PROJECT_ID/jobs/pingwatch-worker:run
  httpMethod: POST
  oauthToken:
    serviceAccountEmail: pingwatch-scheduler@PROJECT_ID.iam.gserviceaccount.com
